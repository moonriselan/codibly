import { FC } from 'react';
import ProductsList from './ProductList';
import './../sass/reset.scss';

const App: FC = () => {
    return (
        <>
            <ProductsList />
        </>
    );
};

export default App;
